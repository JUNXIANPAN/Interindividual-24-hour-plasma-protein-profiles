"""Composition root and application entry point."""

from __future__ import annotations


def main() -> None:
    """Start the application once its transport and runtime are configured."""
    raise NotImplementedError("Application transport is not configured yet.")
